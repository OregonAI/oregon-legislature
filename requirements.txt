# src/ingest_measures.py (PHASE5-MCP-SPEC.md step 4) and the corpus_toolkit
# CLIs (corpus-validate-frontmatter, corpus-verify-provenance, ...).
pyyaml
jsonschema
pypdf
# The [mcp] extra installs the MCP SDK that `corpus-mcp-serve` imports. Without it the CLI
# is on PATH but fails at import, so the container cannot serve — it was missing here.
corpus-toolkit[mcp,semantic] @ git+https://github.com/OregonAI/corpus-toolkit@v1.30.0
