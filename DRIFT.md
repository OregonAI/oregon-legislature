# Drift report

**Generated by `corpus-detect-changes` — do not edit by hand.** Rendered 2026-09-03 from `drift-state.json` and `access-failures.json`; re-render with `corpus-drift-report --config _meta/corpus.yml`. This file replaces the `Source changed:`, `Group drifted:` and `Access failure:` issues a run used to file (corpus-toolkit ADR 0015). It states what a fetch observed and never why.

## Last run

- **Date**: 2026-09-03 · **toolkit**: 1.34.0
- **Verdict**: green — the run compared what it set out to compare
- **Scope**: manifest
- **This run**: 1 in scope · 0 changed · 1 unchanged · 0 seeded · 0 baselines accepted · 0 fetch failed · 0 not comparable

## Changed since baseline (0)

Upstream serves something different from the hash this corpus's manifest records. A source stays here until it is re-ingested and re-seeded, or its baseline is accepted with `--record-baseline=refresh`. The date is when the change was first observed against the current baseline.

_none_

## Groups whose every compared source changed (0)

Correlation, not cause (ADR 0010): the sources changed together, and this report asserts nothing about why. A whole group moving at once has been a template edit, a set of URLs that stopped serving what they did, and a manifest whose baselines had never been recorded.

_none_

## Access failures (0, 0 escalated)

Our fetches of these sources have been failing. A source is **escalated** after 2 consecutive failed runs or 14 elapsed days since the streak began, whichever comes first (ADR 0013). This is a fact about our access: the tool cannot tell a block from a page that moved, and does not guess.

_none_

## Seeded this run (0)

Sources that had no recorded baseline; the run recorded what upstream served so the next run can compare. They were not compared this run.

_none_

## By group

| group | sources | changed | unchanged | fetch failed | no baseline | other |
|---|---|---|---|---|---|---|
| manifest | 1 | 0 | 1 | 0 | 0 | 0 |
